# Public browser playtest

https://kingmadellc.github.io/JimothySurvival/?v=0.6.0

The verified compiled Web output is published on codex/playable-build; editable source stays on main. GitHub Pages publishes the branch root with .nojekyll, using real binaries rather than LFS pointers. Pages reports built at e96fbc9a08928140a22261a5955c50171c95d4b4.

Use Safari in landscape and tap Play. Den → Wardrobe opens the new customization page. The starter bandana is available immediately; other looks require banked finds or banked outings. Wear it equips a look and saves; Coat changes fur color. The first payload is about122MB. Saves belong to this browser/device and origin, with no cloud sync. The existing URL and save identifiers are retained.

Local Safari/WebKit passes18 wardrobe,save,touch and orientation checks; public interaction verification also passes18 checks without runtime errors.

Public0.6.0 Safari/WebKit verification passes18/18, including wardrobe equip,coat,locked look,save/reload and jump.
