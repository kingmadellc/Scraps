const {webkit}=require('playwright');
const fs=require('fs'),path=require('path');
(async()=>{const out=path.resolve('PlaytestCaptures/web-v044-environment');fs.mkdirSync(out,{recursive:true});const browser=await webkit.launch({headless:false});const context=await browser.newContext({viewport:{width:1366,height:1024},hasTouch:true,isMobile:true,deviceScaleFactor:1});const page=await context.newPage();const errors=[];page.on('pageerror',e=>errors.push(e.message));page.on('console',m=>{if(m.type()==='error')errors.push(m.text());});try{
 await page.goto('http://127.0.0.1:8765/?v=0.4.4');await page.locator('#start').click();await page.waitForFunction(()=>!!window.jimothy,{},{timeout:180000});await page.waitForTimeout(1500);await page.screenshot({path:path.join(out,'title.png')});await page.touchscreen.tap(270,635);await page.waitForTimeout(2500);const before=await page.evaluate(()=>window.render_game_to_text());await page.keyboard.down('w');await page.waitForTimeout(1200);await page.keyboard.up('w');await page.waitForTimeout(500);const after=await page.evaluate(()=>window.render_game_to_text());await page.screenshot({path:path.join(out,'avenue.png')});fs.writeFileSync(path.join(out,'states.json'),JSON.stringify({before:JSON.parse(before),after:JSON.parse(after)},null,2));fs.writeFileSync(path.join(out,'errors.json'),JSON.stringify(errors,null,2));console.log(JSON.stringify({before:JSON.parse(before),after:JSON.parse(after),errors}));if(errors.length)process.exitCode=1;
 }finally{await browser.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
